{% extends '::app.html.twig' %}

{% block body -%}
    <h1>Intervenant</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Commentaire</th>
                <td>{{ entity.commentaire }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('intervenants') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('intervenants_edit', { 'id': entity.id, 'idevenement': idevenement}) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
